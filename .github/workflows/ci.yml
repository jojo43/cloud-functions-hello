name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Setup node
        uses: actions/setup-node@v1
        with:
          node-version: 10.x
      - name: Check yarn is installed
        run: yarn -v
